{
  "name": "plato",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel --out-dir dist src",
    "clean": "rm -rf ./dist",
    "watch": "babel -w --out-dir dist src",
    "test": "mocha --compilers js:babel-register --inline-diffs --check-leaks --ui tdd --recursive test",
    "dogfood": "npm run build && ./bin/plato --loglevel silly && ./bin/plato report --loglevel silly",
    "release": "git-dirty && npm run clean && npm run lint && npm test && npm run build",
    "lint": "eslint src test"
  },
  "author": "Jarrod Overson",
  "license": "NONE",
  "dependencies": {
    "async": "^1.5.2",
    "chalk": "^1.1.1",
    "clapi": "^0.9.1",
    "debug": "^2.2.0",
    "eslint": "^2.3.0",
    "extend": "^3.0.0",
    "findup": "^0.1.5",
    "glob": "^6.0.4",
    "handlebars": "^4.0.5",
    "js-schema": "^1.0.1",
    "lodash-template": "^1.0.0",
    "minimist": "^1.2.0",
    "nedb": "^1.6.2",
    "npmlog": "^2.0.0"
  },
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.4.5",
    "babel-core": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "mocha": "^2.3.4"
  }
}
