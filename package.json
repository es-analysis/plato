{
  "name": "plato",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel --out-dir dist src",
    "watch": "babel -w --out-dir dist src",
    "test": "mocha --inline-diffs --check-leaks --compilers js:babel-register --ui tdd --recursive test",
    "generate-coverage": "babel-node ./node_modules/.bin/isparta cover --report text --report html _mocha -- --inline-diffs --check-leaks --ui tdd --reporter dot --recursive test ",
    "dev-report": "npm run build && ./bin/plato"
  },
  "author": "Jarrod Overson",
  "license": "NONE",
  "dependencies": {
    "debug": "^2.1.2",
    "extend": "^3.0.0",
    "findup": "^0.1.5",
    "glob": "^6.0.3",
    "handlebars": "^3.0.3",
    "liftoff": "^2.0.2",
    "minimist": "^1.1.0",
    "nedb": "^1.1.2",
    "out": "^0.5.4",
    "winston": "^0.9.0"
  },
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.3.17",
    "babel-core": "^6.3.26",
    "babel-istanbul": "^0.5.9",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "isparta": "^4.0.0",
    "mocha": "^2.2.1"
  }
}
